# HAL 库结构

`stm32f1xx_hal.h` : 使用HAL库所需要包括的头文件，同时里面包含HAL库的头文件和HAL通用接口

`stm32f1xx_hal_conf.h` : 用来配置所需要使用的外设，可对代码进行一定的剪裁

`stm32f1xx_tim.h` : 某个外设的头文件，包含它才能实现某一类功能，同时还包含了该外设的回调函数

`stm32f1xx.h` ： 关于芯片硬件底层的文件，里面存放了针对芯片的各个寄存器地址和宏定义